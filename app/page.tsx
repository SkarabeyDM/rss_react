/* eslint-disable react-refresh/only-export-components */
import type { Metadata } from 'next';

export const metadata = { title: 'Star Wars Characters' } as Metadata;

export default function MainPage() {
  return <div>Main page</div>;
}
